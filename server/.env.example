APP_ENV=production

CACHE_TTL=60

YF_MIN_INTERVAL=45
YF_MAX_RETRIES=3
YF_BACKOFF_BASE=5
YF_BACKOFF_MAX=120

SGE_REFRESH_INTERVAL=30

API_TIMEOUT=15

# 测试用：强制 yfinance 源返回 429，用于验证退避/冷却/降级（仅测试环境开启）
# YF_FORCE_429=1
