# 复制为 android/key.properties 并填入真实值（该文件已加入 .gitignore，不要提交）。
# 密钥库文件 upload-keystore.jks 放到 android/ 目录下（storeFile 相对 android/ 解析），
# 也可以填绝对路径。生成命令：
#   keytool -genkey -v -keystore upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload
# 务必备份 keystore 文件和密码：丢失后无法给已安装用户推送更新！
storeFile=upload-keystore.jks
storePassword=替换为密钥库密码
keyAlias=upload
keyPassword=替换为密钥密码
